import 'package:flutter/material.dart';

class ShowcaseDetailsPage extends StatelessWidget {
  const ShowcaseDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ShowcaseDetailsPage'),
      ),
      body: const Center(
        child: Text('ShowcaseDetailsPage Page Content'),
      ),
    );
  }
}
