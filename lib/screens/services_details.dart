import 'package:flutter/material.dart';

class ServicesDetailsPage extends StatelessWidget {
  const ServicesDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ServicesDetailsPage'),
      ),
      body: const Center(
        child: Text('ServicesDetailsPage Page Content'),
      ),
    );
  }
}
